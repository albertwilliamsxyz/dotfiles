# TODO

- Add script to replicate configuration in other environments automatically
- Abstract information about the host in variables, the only thing I care about is how it works, details should be provided by the host when installing

# Nice to have

- Create client for instalation
- Add another script to install dependencies
